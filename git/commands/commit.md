---
description: Créer des commits bien formatés avec format conventional et emoji
argument-hint: "[message] [--verify] [--no-push]"
---
# git:commit

Créer des commits bien formatés avec format conventional et emoji

---

**IMPORTANT**: Use the Skill tool to invoke the skill `git:commit`.

- If $ARGUMENTS is empty, contains only `.`, or contains only dots, invoke the skill WITHOUT any args parameter
- Otherwise, invoke the skill with args: $ARGUMENTS

Execute the skill immediately. Do not explain or describe what you will do - just invoke the skill using the Skill tool.

---

**Note**: This slash command was auto-generated to workaround Claude Code bug #15178.
Once fixed, this workaround can be removed.
