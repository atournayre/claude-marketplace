---
description: Génère un Architecture Decision Record (ADR) formaté et structuré
argument-hint: "titre"
allowed-tools:
  - TaskCreate
  - TaskUpdate
  - TaskList
  - Read
  - Write
  - Edit
  - Bash
---
# doc:adr

Génère un Architecture Decision Record (ADR) formaté et structuré

---

**IMPORTANT**: Use the Skill tool to invoke the skill `doc:adr` with arguments: $ARGUMENTS.

Execute the skill immediately. Do not explain or describe what you will do - just invoke the skill using the Skill tool.

---

**Note**: This slash command was auto-generated to workaround Claude Code bug #15178.
Once fixed, this workaround can be removed.
