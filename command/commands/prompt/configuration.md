---
description: Génère un prompt pour configuration et feature flags
allowed-tools:
  - Read
  - Write
  - Bash
  - AskUserQuestion
---
# prompt:configuration

Génère un prompt pour configuration et feature flags

---

**IMPORTANT**: Use the Skill tool to invoke the skill `prompt:configuration` without arguments.

Execute the skill immediately. Do not explain or describe what you will do - just invoke the skill using the Skill tool.

---

**Note**: This slash command was auto-generated to workaround Claude Code bug #15178.
Once fixed, this workaround can be removed.
